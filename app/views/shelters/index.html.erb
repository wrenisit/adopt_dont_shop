<!DOCTYPE html>
<html>
<head>
<style>
body {
  background-color: rgb(241, 140, 104);
  color: white;
}
</style>
</head>
<body>
<h1>All Shelters</h1>
<h2><%= link_to "Pet Index", '/pets' %></h2>

<% @shelters.each do |shelter| %>
  <h2><%= link_to "#{shelter.name}", "/shelters/#{shelter.id}" %>
</h2>
  <%= link_to 'Update', "/shelters/#{shelter.id}/edit" %>
  <%= link_to 'Delete', "/shelters/#{shelter.id}", method: :delete %>


<% end %>
<%= button_to 'Add A Shelter', "/shelters/new" %>
</body>
