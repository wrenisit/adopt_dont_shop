<h1>All Shelters</h1>

<% @shelters.each do |shelter| %>
  <h2><%= shelter.name %></h2>
  <%= link_to 'Update', "/shelters/#{shelter.id}/edit" %>

<% end %>
<%= button_to 'Add A Shelter', "/shelters/new" %>
